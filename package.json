{
    "name": "@ac/monorepo",
    "private": true,
    "scripts": {
        "dev": "pnpm --filter \"./apps/**\" run dev",
        "build": "pnpm --filter \"./apps/**\" run build",
        "check": "pnpm --filter \"./apps/**\" run check"
    },
    "devDependencies": {
        "typescript": "^5.9.3"
    }
}